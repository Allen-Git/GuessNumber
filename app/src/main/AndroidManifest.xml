<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
      package="com.allen.android.guess1"
      android:versionCode="3"
      android:versionName="1.0">

    <application android:icon="@drawable/icon" android:label="@string/app_name">
        <activity android:name=".GuessNumber"
                  android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name="com.allen.android.guess1.Game" android:label="@string/single_match" android:configChanges="orientation"></activity>
        <activity android:name="com.allen.android.guess1.RankList" android:label="@string/rank"></activity>
        <activity android:name="com.allen.android.guess1.About" android:label="@string/about"></activity>
        
        <!-- Admob with Google -->
        <!--<activity android:name="com.google.ads.AdActivity"
                android:configChanges="keyboard|keyboardHidden|orientation|screenLayout|uiMode|screenSize|smallestScreenSize" />
        -->
    </application>

    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE"/>
    <uses-permission android:name="android.permission.INTERNET"/>
</manifest>